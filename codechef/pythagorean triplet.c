#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main()
{
    int n,m=1,k=1;
    scanf("%d",&n);

    for(m=1){
        if(n==sqrt(m*m+k*k))||(m==sqrt(n*n+k*k))||(k==sqrt(n*n+m*m))){

        }

    }
    printf("%d %d",m,k);


    return 0;
}

